# 2026-09-21 视觉重排

本轮依照用户最新指令取代旧的青绿石材风格。用户否定第一版浅粉色块及四等分导航后，重新调整配色、比例和页面布局。

- 内容橙红 `#ff512f`，研究蓝紫 `#5945dc`，资料冰蓝 `#b9e5f3`，待办雾绿 `#a3b9a6`。
- 内容主卡跨两行；研究占右上，资料和待办并排放右下。中文标签、当前状态和真实数量保留。
- 客户和报告使用开放列表；设置使用账号表单双列与全宽数据源；跟进页使用录入与记录双列。窄屏依次展开。
- 统一深色胶囊控件；保留阅读区域、焦点样式、浅色主题和减少动画支持。
- 同步登录、注册及三个独立报告页面，更新 CSS 缓存版本。

## 验证

- Vitest：33 文件、257 测试通过。
- 实际登录浏览器检查：工作区、客户列表、设置、跟进记录；使用现有 HPV 内容显示 100 个关键词，没有填写或提交业务表单。
- 隔离浏览器使用标明“布局验收”的模拟 API，检查工作区 390 / 768 / 1024 / 1512px 布局和四个入口切换，无页面异常、无文档横向溢出；客户、报告、跟进、设置在390px检查无文档横向溢出。模拟数据仅存在浏览器拦截中，不写服务器。
- 登录、注册和独立报告在390px无文档横向溢出。
- 四种色块与其文字对比度分别为5.35、5.75、12.86、8.30；深色控件文字13.20，均高于普通文字4.5门槛。此为指定配色测量，不代表全站所有内容已通过完整无障碍认证。
